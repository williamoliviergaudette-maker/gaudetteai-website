{
  "name": "gaudetteai-construction",
  "version": "1.0.0",
  "description": "Construction page for Gaudette AI",
  "scripts": {
    "dev": "cd public && python -m http.server 3000",
    "build": "if not exist dist mkdir dist && copy public\\index.html dist\\index.html",
    "preview": "cd dist && python -m http.server 4173"
  }
}
